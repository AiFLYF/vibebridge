# /init 行为规范

## 唯一目标

建立基础设施。**结束。**

```bash
python core/vb.py init
```

## 绝对禁止

初始化过程中**不得询问任何内容**：

```
姓名        年龄        诊断        症状
兴趣        社交能力     性格        家庭情况
心理状态     能力评分     使用目的     期望目标
```

不得出现任何形式的：

> "为了更好地帮助你，请回答以下问题……"

不得设计 onboarding 问卷、能力自评、兴趣勾选或任何形式的信息采集表单。

**用户画像只能从后续的自然交互中生长出来。** 这是整个系统的设计前提，不是可以权衡的偏好。

## 实际执行的操作

```
创建数据目录        ~/.vibebridge/
创建子目录          memory sessions projects experiments milestones
                    growth analytics reports backups exports logs
创建默认 schema     13 个 JSON 文件，全部使用 schema 默认值
创建不可变日志      memory/observations.jsonl（空文件）
检查待迁移版本      有则自动迁移，迁移前自动备份
环境自检            Python 版本、目录可写、UTF-8 输出
安全引擎自检        措辞拦截 + 危机识别是否正常工作
写入初始化日志
```

## 幂等

重复运行安全。已存在的文件进入 `kept`，不会被覆盖或重置。

```json
{
  "ok": true,
  "already_initialized": true,
  "created": [],
  "kept": ["version", "config", "profile", "..."]
}
```

用户重装或升级 Skill 后重新 `/init`，历史数据完整保留。

## 初始化后对用户说什么

一句话，然后回到用户本来想做的事。

好：

> 好了。你想做点什么？

也可以：

> 初始化完成，数据存在 `~/.vibebridge/`。接下来正常用就行。

不好：

> 太好了！现在让我们开始你的成长之旅！为了给你提供最好的个性化支持，我想先了解一下你的情况……

## 首次对话之后

从第一次真正的交互开始，观察引擎就开始工作，但用户不需要知道，也不需要感觉到。

不要说"我已经开始记录了"。不要解释系统在做什么。除非用户主动问。

## 环境检查失败时

| 情况 | 处理 |
|---|---|
| 目录不可写 | 报告路径与错误，提示用 `VIBEBRIDGE_HOME` 指定其他位置 |
| Python 版本过低 | 报告版本要求（3.9+） |
| 安全自检失败 | **报告并停止**，不要在安全引擎异常的状态下继续 |
| 检测到旧版本数据 | 自动迁移，迁移前自动快照，失败自动回滚 |

